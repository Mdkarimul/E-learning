export interface authkey {
    message: string;
    auth_key:string
}

export interface login {
    message : string,
    access_token : string
}

export interface signup {
    message : string,
    access_token : string
}
export interface resetpassword {
    message :string
}

